import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ubb.bletracker',
  appName: 'ble-tracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
